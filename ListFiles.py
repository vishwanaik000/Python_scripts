import os
arr = os.listdir("D:")
arr #prints files and folder in a dir in a ame line 
#this prints all files in a dir 
'''path = "D:"
for path, dirs, files in os.walk(path):
  print path
  for f in files:
    print f '''
